{
  "name": "auth-custom-advanced-manager",
  "version": "0.0.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node ./dist/index.js",
    "format": "prettier --cache --write .",
    "format-check": "prettier --cache --check ."
  },
  "dependencies": {
    "@interopio/manager": "^2.0.0",
    "@okta/jwt-verifier": "^4.0.1"
  },
  "devDependencies": {
    "prettier": "^3.6.1",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  }
}
