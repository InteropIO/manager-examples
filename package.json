{
  "name": "manager-examples",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "examples/*"
  ],
  "scripts": {
    "format": "prettier --cache --write .",
    "format-check": "prettier --cache --check ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
